services:
  app:
    build:
      context: .
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      APP_SUPABASE_URL: ${APP_SUPABASE_URL:?set APP_SUPABASE_URL}
      APP_SUPABASE_ANON_KEY: ${APP_SUPABASE_ANON_KEY:?set APP_SUPABASE_ANON_KEY}
